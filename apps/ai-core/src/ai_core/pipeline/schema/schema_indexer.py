from typing import List


class SchemaIndexer:
    def upsert(self, tenant_id: str, fields: List[str]) -> None:
        """Placeholder; will delegate to document_service/qdrant upserts."""
        return None
